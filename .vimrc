set clipboard=unnamed // Enables sharing the system clipboard with Vim, allowing you to copy and paste between Vim and other applications
set relativenumber // show relative line numbers of file
set showcmd // shows partial commands in the bottom right of the screen as you type them
set cursorline Highlights the line on which the cursor is currently located
set tabstop=4 // Sets the number of spaces a tab character represents
set shiftwidth=4 Sets the number of spaces used for autoindentation
set expandtab // Converts tabs to spaces when editing
set smartindent // Automatically indents new lines to the same level as the previous line
set nowrap // Disables line wrapping (lines extend off-screen)
set mouse=a // Enables mouse support for all modes
syntax on // Enables syntax highlighting
